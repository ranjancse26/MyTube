<UserControl xmlns:Controls1="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"  
    x:Class="MyTube.YouTubeButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="480" d:DesignWidth="480">

	<Button x:Name="Button" toolkit:TiltEffect.IsTiltEnabled="True" Margin="0" BorderThickness="1">
		<Grid Background="{StaticResource PhoneInactiveBrush}">
			<Image Stretch="UniformToFill" x:Name="Image" Opacity="0.65" RenderTransformOrigin="0.573,0" />
			<Image x:Name="PlayImage" VerticalAlignment="Center" HorizontalAlignment="Center" />
		</Grid>
    </Button>
      
</UserControl>
